# ===========================================
# vibe-stack Environment Variables
# ===========================================
# Copy this file to .env.development.local:
#   cp .env.example .env.development.local
#
# Everything is optional — the app works without any of these.
# Only set what you need.

# --- AI (optional) ---
# App works without these — just no AI features
# OpenAI: https://platform.openai.com/ → API Keys
OPENAI_API_KEY=
# Override model (defaults to gpt-5.2, falls back to gpt-4o)
# OPENAI_MODEL=gpt-4o

# Anthropic: https://console.anthropic.com/ → API Keys
ANTHROPIC_API_KEY=
# Override model (defaults to claude-opus-4-6, falls back to claude-sonnet-4-5)
# ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# --- Database (optional) ---
# App works without this — just no database features
# Works with any Postgres: Neon, Supabase, Railway, local, etc.
# Neon (recommended): https://neon.tech/ → Create project → Connection string
# Supabase: https://supabase.com/ → Project → Settings → Database → Connection string
DATABASE_URL=

# --- Auth (optional) ---
# App works without these — fully open, no sign-in required
# Clerk: https://clerk.com/ → Create app → API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# --- Analytics (optional) ---
# App works without these — no analytics tracking
# PostHog: https://posthog.com/ → Project → Settings → Project API Key
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# --- SEO (optional) ---
# Used for sitemap, robots.txt, and OpenGraph metadata
# Recommended to set in production (for example: https://yourdomain.com)
NEXT_PUBLIC_BASE_URL=
