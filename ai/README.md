# AI Assets

This folder is for AI prompt templates and configuration.

- Place prompt templates in `prompts/`
- See `src/lib/ai.ts` for the AI helper functions

If your project doesn't use AI, you can ignore this folder.
